name: pylinter
on:
  push:
    paths: "*.py"
jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: clone the code
        uses: actions/checkout@V4
      - name: install linter
        run: pip install ruff
      - name: checking lint
        run: ruff check .
        
     
       
