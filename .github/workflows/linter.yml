name: pylinter
on:
  push:
    paths: "*.py"
jobs:
  code:
    runs-on: uubuntu-latest
    steps:
      - name: clone the code
        uses: actions/checkout@V7
      - name: install linter
        run: pip install ruff
      - name: checking lint
        run: ruff check .
        
     
       
